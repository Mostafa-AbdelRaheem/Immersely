services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: immersely
      POSTGRES_PASSWORD: immersely12345
      POSTGRES_DB: immersely_db
    ports:
      - "5440:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: